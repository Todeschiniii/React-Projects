// HOW TO STYLE REACT COMPONENTS WITH CSS
// --------------------------------------
// (not including external frameworks or preprocessors)

// 1. EXTERNAL ( importando CSS externo )
// 2. MODULES ( styles.module.css )
// 3. INLINE ( estilo diretamente no elemento )

import Button from '../my-react-classes 🎓/3/Button/Button.jsx'

function App() {
    return (
        <div>
            <Button />
        </div>
    );
}

export default App
